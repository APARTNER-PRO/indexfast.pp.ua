# ── Production build (npm run build)
# Відносний шлях /api — працює і на indexfast.pp.ua і на indexfast.local
# Apache (.htaccess) проксіює /api/* → test.developer.pp.ua server-side
VITE_API_URL=/api
